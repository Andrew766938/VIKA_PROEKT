# 🍽️ Platter Flow - Restaurant Management System

Полнофункциональная система управления рестораном с веб-интерфейсом и RESTful API.

## 📋 Описание

Platter Flow - это современная система для управления ресторанной деятельностью, включающая:
- 👥 Управление персоналом (официанты, повара, администраторы)
- 📋 Управление меню
- 🪑 Управление столами
- 🛒 Управление заказами
- 🔐 Аутентификацию пользователей
- 📊 Статистику и аналитику

## 🏗️ Архитектура проекта

```
app/
├── api/              # API маршруты (роутеры)
│   ├── auth.py       # Аутентификация
│   ├── menu.py       # Управление меню
│   ├── tables.py     # Управление столами
│   ├── orders.py     # Управление заказами
│   └── employees.py  # Управление сотрудниками
├── models/           # SQLAlchemy модели
│   ├── user.py       # Модель пользователя
│   ├── menu.py       # Модель меню
│   ├── table.py      # Модель стола
│   └── order.py      # Модель заказа
├── schemes/          # Pydantic схемы (валидация)
│   ├── user.py
│   ├── menu.py
│   ├── table.py
│   └── order.py
├── database/         # Конфигурация БД
│   └── core.py
├── config.py         # Конфигурация приложения
└── dependencies.py   # Зависимости

static/
├── index.html        # Веб-интерфейс
└── js/
    └── app.js        # Логика приложения
```

## 🚀 Установка и запуск

### Требования
- Python 3.8+
- pip или uv

### Установка зависимостей

```bash
pip install -r requirements.txt
```

Отдельно установить зависимости:
```bash
pip install fastapi uvicorn sqlalchemy passlib bcrypt pydantic
```

### Запуск сервера

```bash
python main.py
```

Сервер запустится на http://localhost:8000
Веб-интерфейс доступен по адресу http://localhost:8000/static/index.html

## 📚 API Документация

### Автентификация

#### POST `/api/auth/login`
Вход в систему
```json
{
  "username": "ofikNum1",
  "password": "123321"
}
```
**Ответ:**
```json
{
  "id": 1,
  "username": "ofikNum1",
  "full_name": "Иван Петров",
  "role": "waiter",
  "message": "Login successful"
}
```

#### POST `/api/auth/register`
Регистрация нового пользователя
```json
{
  "username": "newuser",
  "password": "password123",
  "full_name": "Имя Фамилия",
  "role": "waiter"
}
```

### Меню

#### GET `/api/menu/`
Получить все пункты меню

#### GET `/api/menu/{item_id}`
Получить конкретный пункт меню

#### POST `/api/menu/`
Создать новый пункт меню
```json
{
  "name": "Салат Цезарь",
  "description": "Классический салат с курицей",
  "price": 450.00,
  "category": "Салаты",
  "is_available": true
}
```

#### PUT `/api/menu/{item_id}`
Обновить пункт меню

#### DELETE `/api/menu/{item_id}`
Удалить пункт меню

### Столы

#### GET `/api/tables/`
Получить все столы

#### GET `/api/tables/{table_id}`
Получить конкретный стол

#### POST `/api/tables/`
Создать новый стол
```json
{
  "table_number": 1,
  "seats": 4
}
```

#### PUT `/api/tables/{table_id}`
Обновить информацию о столе
```json
{
  "seats": 6,
  "is_occupied": false
}
```

#### DELETE `/api/tables/{table_id}`
Удалить стол

### Заказы

#### GET `/api/orders/`
Получить все заказы

#### GET `/api/orders/status/{status}`
Получить заказы по статусу (pending, confirmed, ready, completed, cancelled)

#### GET `/api/orders/{order_id}`
Получить конкретный заказ

#### POST `/api/orders/`
Создать новый заказ
```json
{
  "table_id": 1,
  "items": [
    {"menu_item_id": 1, "quantity": 2},
    {"menu_item_id": 3, "quantity": 1}
  ]
}
```

#### PUT `/api/orders/{order_id}`
Обновить статус заказа
```json
{
  "status": "ready"
}
```

#### DELETE `/api/orders/{order_id}`
Удалить заказ

### Сотрудники (для админа)

#### GET `/api/employees/`
Получить всех сотрудников

#### GET `/api/employees/role/{role}`
Получить сотрудников по роли (waiter, chef, admin)

#### GET `/api/employees/{employee_id}`
Получить информацию о сотруднике

#### POST `/api/employees/`
Создать нового сотрудника
```json
{
  "username": "newwaiter",
  "password": "password123",
  "full_name": "Петр Иванов",
  "role": "waiter"
}
```

#### PUT `/api/employees/{employee_id}`
Обновить информацию о сотруднике
```json
{
  "full_name": "Петр Иванович Иванов",
  "password": "newpassword123",
  "is_active": true
}
```

#### DELETE `/api/employees/{employee_id}`
Удалить сотрудника

## 👥 Тестовые учетные данные

| Роль | Логин | Пароль | Функции |
|------|-------|--------|----------|
| Официант | ofikNum1 | 123321 | Просмотр меню, столов, заказов |
| Повар | povarNum1 | 123321 | Просмотр заказов |
| Администратор | adminNum1 | 123321 | Полный доступ + управление сотрудниками |

## 🔐 Роли и права

- **Waiter (Официант)**: Может просматривать меню, столы и создавать заказы
- **Chef (Повар)**: Может просматривать заказы и обновлять их статусы
- **Admin (Администратор)**: Полный доступ ко всем функциям

## 🛠️ Технологический стек

- **Backend**: FastAPI, SQLAlchemy, Uvicorn
- **Frontend**: HTML5, CSS3, Vanilla JavaScript
- **Database**: SQLite
- **Authentication**: Bcrypt, Passlib
- **Validation**: Pydantic

## 📝 Структура кода

### Models (app/models/)
SQLAlchemy ORM модели для работы с БД:
- **User**: пользователи системы
- **MenuItem**: пункты меню
- **RestaurantTable**: столы ресторана
- **Order**: заказы

### Schemes (app/schemes/)
Pydantic схемы для валидации данных:
- Валидация входящих данных API
- Сериализация данных в JSON
- Типизация данных

### API Routes (app/api/)
FastAPI маршруты для каждой сущности:
- RESTful методы (GET, POST, PUT, DELETE)
- Обработка ошибок
- Логирование операций

## 🌐 Веб-интерфейс

### Особенности UI:
- ✅ Отзывчивый дизайн
- ✅ Темная и светлая темы
- ✅ Модальные окна для создания/редактирования
- ✅ Статистика в реальном времени
- ✅ Автообновление данных каждые 5 сек

## 🔧 Развертывание

### Локальное развертывание
```bash
python main.py
```

### Production (с Gunicorn)
```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:8000 main:app
```

## 📊 Примеры запросов

### cURL примеры

**Вход:**
```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"ofikNum1","password":"123321"}'
```

**Создание заказа:**
```bash
curl -X POST http://localhost:8000/api/orders/ \
  -H "Content-Type: application/json" \
  -d '{"table_id":1,"items":[{"menu_item_id":1,"quantity":2}]}'
```

**Получение всех столов:**
```bash
curl http://localhost:8000/api/tables/
```

## 📄 Лицензия

MIT License

## 👨‍💼 Автор

Painter Flow Development Team

## 📞 Поддержка

Для вопросов и проблем создайте Issue в репозитории.
